void main() {
    int a = 5;
    int j;
    int b = 3, c = 4;

    int c = a + b;
    int d = a * b;

    int resultado1 = (a + b) * (a - b);
    int resultado2 = a * (b / 2);

    if ((a > b) && (c <= d) || (a < b)) {
        a = a - 2;
    } else {
        b = a + 4;
    }

    int i = 0;
    while (i < 5) {
        i = 1 + a;
    }

    for (int j = 0; j < 3; j = j + 1) {
        a = b + c;
    }
}